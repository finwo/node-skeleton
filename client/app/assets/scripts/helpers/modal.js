define(function() {
  return function($el) {
    //$el
    //  .on('click', function() { document.location.hash = '#!'; })
    //  .find('.modal-content')
    //  .on('click', function() { return false; })
  };
});
